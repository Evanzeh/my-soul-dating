name: my_soul
description: Kenyan dating app for real connections
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.13.6
  provider: ^6.1.1
  flutter_card_swiper: ^6.1.0
  cached_network_image: ^3.3.0

flutter:
  uses-material-design: true
